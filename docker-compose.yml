# Moldmaker on the NAS. Host port 8081 → container 80 (8080 is Jiggmaker).
version: "3.3"

services:
  moldmaker:
    build: .
    image: moldmaker:local
    container_name: moldmaker
    ports:
      - "8081:80"
    restart: unless-stopped
    mem_limit: 256m
